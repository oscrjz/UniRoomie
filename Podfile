# Define the minimum platform version for your project
platform :ios, '13.0'

target 'UniRoomie' do
  # Use dynamic frameworks
  use_frameworks!

  # Pods for UniRoomie
  pod 'Firebase/Firestore'

  target 'UniRoomieTests' do
    inherit! :search_paths
    # Add test-specific pods here if needed
  end

  target 'UniRoomieUITests' do
    inherit! :search_paths
    # Add UI test-specific pods here if needed
  end
end
